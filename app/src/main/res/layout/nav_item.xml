<?xml version="1.0" encoding="utf-8"?>
<com.twohrgrocery.customer.utility.CheckableConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <!-- Icon -->
    <ImageView
        android:id="@+id/nav_item_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center"
        android:layout_marginStart="16dp"
        android:layout_marginLeft="16dp"
        android:duplicateParentState="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription"
        tools:src="@drawable/ic_account_circle_black_96dp" />

    <!-- Expansion Indicator -->
    <ImageView
        android:id="@+id/nav_item_expand"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"
        android:scaleType="center"
        app:layout_constraintBottom_toBottomOf="@+id/nav_item_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@+id/nav_item_icon"
        tools:ignore="ContentDescription"
        tools:src="@drawable/ic_keyboard_arrow_down_black_24dp" />

    <!-- Name -->
    <TextView
        android:id="@+id/nav_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:duplicateParentState="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/nav_item_expand"
        app:layout_constraintLeft_toRightOf="@+id/nav_item_icon"
        app:layout_constraintRight_toLeftOf="@+id/nav_item_expand"
        app:layout_constraintStart_toEndOf="@id/nav_item_icon"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Test" />

    <!-- Border -->
    <View
        android:id="@+id/nav_item_border"
        android:layout_width="0dp"
        android:layout_height="1dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintStart_toStartOf="parent" />


</com.twohrgrocery.customer.utility.CheckableConstraintLayout>