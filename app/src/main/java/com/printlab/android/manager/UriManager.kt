package com.printlab.android.manager

import android.content.Context

import java.io.File

object UriManager {

   /* fun trackUrl(config: ConfigObj?, track: TrackObj?): String {
        return when (track?.type) {
            TrackObj.Type.AUDIO -> config?.audioPath + track.parentId + File.separator + track.url
            TrackObj.Type.VIDEO -> config?.videoPath + track.parentId + File.separator + track.url
            TrackObj.Type.PDF -> config?.pdfPath + track.parentId + File.separator + track.url
            else -> ""
        }
    }

    fun trackPath(context: Context, track: TrackObj?): String {
        return context.filesDir.absolutePath + File.separator + track?.url
    }

    fun trackThumbUrl(config: ConfigObj?, track: TrackObj?): String {
        return when (track?.type) {
            TrackObj.Type.AUDIO -> config?.audioPath + track.parentId + File.separator + track.thumb
            TrackObj.Type.VIDEO -> config?.videoPath + track.parentId + File.separator + track.thumb
            TrackObj.Type.PDF -> config?.pdfPath + track.parentId + File.separator + track.thumb
            else -> ""
        }
    }

    fun seriesThumbUrl(config: ConfigObj?, track: SeriesObj?): String {
        return when (track?.type) {
            TrackObj.Type.AUDIO -> config?.audioPath + track.id + File.separator + track.thumb
            TrackObj.Type.VIDEO -> config?.videoPath + track.id + File.separator + track.thumb
            TrackObj.Type.PDF -> config?.pdfPath + track.id + File.separator + track.thumb
            else -> ""
        }
    }*/

}